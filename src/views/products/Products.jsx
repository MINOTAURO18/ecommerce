import styles from './products.module.css'
const Products = () => {
    return(
        <section className={`${styles.products}`}>
            hola
        </section>
    )
}

export default Products;